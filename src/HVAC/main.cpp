#include<algorithm>
#include<iostream>
#include<fstream>
#include<vector>
using namespace std;

const int ZONE_NUM = 20;
vector<double> curr_temperatures(ZONE_NUM,0);
const double MAX_TEMP = 27;
const double MIN_TEMP = 24;

bool check_confortable()
{
    for(int i=0;i<ZONE_NUM;++i)
    {
        
    }
}
